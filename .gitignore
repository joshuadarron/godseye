# Environment variables
# Matches at every level: the root .env (compose interpolation + the shared
# JWT_SECRET) as well as each service's own.
.env

# Local dev scratch — Go build cache (see mise.toml) and compiled dev binaries.
.dev/

# Go
services/*/server
services/*/server.exe
go.work.sum
*.exe
*.dll
*.so
*.dylib

# Node / Frontend
node_modules/
dist/
dist-ssr/
*.local

# Generated aircraft databases (large, built from OpenSky data via scripts/build-aircraft-db.mjs)
packages/frontend/public/data/aircraft.json
packages/frontend/public/data/aircraft-detail.json
scripts/.*.tmp

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# Editors
.vscode/*
!.vscode/extensions.json
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# OS
.DS_Store
Thumbs.db
